<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PFinalDM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/my_primary</item>
    </style>


    <style name="Theme.Cargando" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/logoanime</item>
        <item name="windowSplashScreenAnimationDuration">5000</item>
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.PFinalDM</item>
    </style>

    <style name="Theme.PFinalDM" parent="Base.Theme.PFinalDM" />
</resources>